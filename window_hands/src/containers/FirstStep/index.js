import React from 'react';
import {Link} from 'react-router-dom';
const firstStep = (props) => {
    return(
        <div>
            <Link to='/controller' >Control a Window</Link>
        </div>
    );
};
export default firstStep;